source "https://rubygems.org"

# Use the same Jekyll + plugin set that GitHub Pages runs server-side.
gem "github-pages", group: :jekyll_plugins

# Required for `bundle exec jekyll serve` on Ruby 3+ (webrick left stdlib).
gem "webrick", "~> 1.8"
