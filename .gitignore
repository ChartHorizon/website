# macOS
.DS_Store

# Jekyll
_site/
.jekyll-cache/
.jekyll-metadata

# Brainstorming visual companion
.superpowers/

# Local redesign preview harness (static mirror of the layouts for screenshotting)
.preview/

# Local agent tooling — no reason to ship it with the site
.claude/
.impeccable/
.github/skills/
.github/agents/
.github/hooks/

# Internal working notes. This repo is PUBLIC: product positioning and the design
# system stay local, so a stray `git add -A` can't publish them.
DESIGN.md
PRODUCT.md

# Wrangler's local deploy state. Written by `ops/website-deploy.sh` and holds the
# Cloudflare account id — not a credential, but account metadata, and this repo is public.
.wrangler/

# Isolated worktrees for in-progress work (see superpowers:using-git-worktrees).
.worktrees/
